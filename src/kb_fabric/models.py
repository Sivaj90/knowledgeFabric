"""SQLAlchemy ORM models for the metadata store.

Populated in Phase 1.2 (documents/chunks schema). Empty scaffold for now so
alembic autogenerate has a stable target_metadata import path from day one.
"""

from kb_fabric.db import Base

__all__ = ["Base"]
