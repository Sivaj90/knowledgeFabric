# Landmark Knowledge Fabric - Slice 1 (capture pipeline) dependencies
# Pinned to versions available/tested as of 2026-08-20.

# --- Config / env ---
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Postgres / ORM / migrations ---
SQLAlchemy==2.0.35
psycopg[binary]==3.2.3
pgvector==0.3.6
alembic==1.13.3

# --- Queue ---
celery==5.4.0
redis==5.1.1

# --- Ingestion / chunking ---
# unstructured pinned to 0.16.25 (bumped from 0.15.13 during Phase 1.4 -- the
# older version's pdf extra pulled in a pdfminer.six/pdfplumber combo that
# both failed to import (`libGL.so.1` missing -- fixed via `dnf install
# mesa-libGL`; then `cannot import name 'PSSyntaxError'` -- fixed by
# upgrading unstructured itself rather than hand-pinning pdfminer down,
# which just traded one conflict for another with pdfplumber's own pin).
unstructured[docx,pptx,pdf,md]==0.16.25
langchain==0.3.7
langchain-text-splitters==0.3.2

# --- LLM client (LiteLLM proxy speaks OpenAI-compatible API) ---
# openai bumped from 1.54.4 during Phase 1.4 -- that version's bundled
# httpx client usage passed a `proxies` kwarg that newer httpx (0.28.1,
# pulled in transitively) no longer accepts, breaking every API call with
# `TypeError: Client.__init__() got an unexpected keyword argument
# 'proxies'`. Confirmed live: 1.54.4 broken, current (3.3.1 at time of
# fix) works against the same httpx version.
openai>=3.0.0

# --- Query API (Slice 2, Phase 2.7) ---
fastapi==0.115.5
uvicorn[standard]==0.32.1

# --- Testing ---
pytest==8.3.3
pytest-mock==3.14.0
# reportlab: test-fixture generation only (Phase 1.6 -- generates real
# .pdf test fixtures, including a scanned/image-only one for OCR testing).
# Not used by application code.
reportlab==5.0.0
